{
  "name": "eslint-config",
  "private": true,
  "scripts": {
    "test": "jest",
    "lint": "eslint packages/**/*.js packages/**/*.json"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {},
  "version": "0.1.0"
}
